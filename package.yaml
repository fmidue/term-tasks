name: term-tasks
extra-source-files: []
dependencies:
  - base
  - split
  - QuickCheck
  - output-monad
default-extensions:
  - DuplicateRecordFields
  - OverloadedLabels
library:
  source-dirs: src
  exposed-modules:
    - TermTasks.Direct
    - TermTasks.Perturbed
    - TermTasks.Records
    - TermTasks.DataType
  dependencies:
    - extra

  ghc-options:
    - -Wall
tests:
  term-tasks-test:
    main: Spec.hs
    source-dirs:
      - test
    ghc-options:
      - -threaded
      - -rtsopts
      - -with-rtsopts=-N
    dependencies:
      - term-tasks
      - hspec
